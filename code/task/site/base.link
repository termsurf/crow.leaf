
task host
  note <Start server>

task free

task fork

task work
  note <Process request>

  - walk routes by path nodes
  - find the route and invoke the handler
    - check method and url
    - if show, respond with the show html
    - if task, respond with calling the task
    - if move, respond with redirect
