
load @drumwork/seed/code/form/bind
  find form bind

load @drumwork/seed/code/form/link
  find form link

load @drumwork/seed/code/form/term
  find form term

form like
  case base-like
  case task-like

form base-like
  take name, like term
  take bind, list bind

form task-like
  take head, mesh link, term name
  take take, mesh link, term name
  take free, mesh link, term name
